import 'package:flutter/material.dart';

class GroupEntry extends StatelessWidget {
  const GroupEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Group Entry');
  }
}
